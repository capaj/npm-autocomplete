{
  "name": "npm-autocomplete",
  "main": "./lib/npm-autocomplete",
  "version": "0.1.1",
  "description": "atom editor autocomplete-plus provider for writing require statements to npm packages",
  "repository": "https://github.com/capaj/npm-autocomplete",
  "license": "MIT",
  "engines": {
    "atom": ">=0.174.0 <2.0.0"
  },
  "dependencies": {
    "atom-package-dependencies": "latest",
    "fuzzaldrin": ">=2.1.0"
  },
  "package-dependencies": {
    "autocomplete-plus": ">=2.5.0"
  },
  "providedServices": {
    "autocomplete.provider": {
      "versions": {
        "2.0.0": "provide"
      }
    }
  }
}
